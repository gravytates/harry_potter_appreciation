<h1>New Review</h1>

<%= render "layouts/errors", :object => @review %>

<%= form_for [@character, @review] do |f| %>
  <%= f.label :author%>
  <%= f.text_field :author %>
  <%= f.label :body%>
  <%= f.text_field :body %>

  <%= f.submit %>
<% end %>
